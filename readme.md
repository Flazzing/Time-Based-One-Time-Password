Note: Make sure to have PIP install

Command to execute the activation of virtual env file
 .\venv\Scripts\activate

1. The first program generate a qrcode that generates the totp (works with google authenticator)

2. The second program takes the same key from qrcode, and generate the totp. 



